import React from 'react';

const App: React.FC = () => {
  return (
    <div
      style={{
        width: '100vw',
        height: '100vh',
        background: '#ffffff',
        margin: 0,
        padding: 0,
        display: 'block',
      }}
      aria-hidden="true"
      role="presentation"
    >
      {/* Intentionally blank — public site shows a simple white screen */}
    </div>
  );
};

export default App;
